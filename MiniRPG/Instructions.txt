You are GitHub Copilot inside Visual Studio 2022.

Modify MapViewModel.cs.

Requirements:
• Add properties:
   - int CurrentDay => TimeService.Day
   - string TimeOfDay => TimeService.GetTimeOfDay()
• Raise OnPropertyChanged when time advances.
• After any RestCommand, Travel, or Battle → call TimeService.AdvanceHours(x)
   - Rest = 8 hours
   - Battle = 1 hour
   - Travel = 2 hours
• Call OnPropertyChanged(nameof(CurrentDay)) and OnPropertyChanged(nameof(TimeOfDay)).
• Add TODO:
   // Add time-based events and NPC schedules

---

After you finish, update the Readme.md file with the changes.