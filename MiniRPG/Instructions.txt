You are GitHub Copilot inside Visual Studio 2022.

Modify NPC.cs.

Requirements:
• Add properties:
   - int AvailableStartHour
   - int AvailableEndHour
   - string CurrentLocation   // e.g., "Town Square", "Home", "Shop"
• Add method IsAvailableNow():
   - return TimeService.Hour >= AvailableStartHour && TimeService.Hour < AvailableEndHour;
• Example NPC initialization:
   new NPC("Mira", "QuestGiver", "Good to see you!") {
       AvailableStartHour = 8,
       AvailableEndHour = 20,
       CurrentLocation = "Market Square"
   };
• Add TODO:
   // Add weekly schedules and holiday routines later

---

After you finish, update the Readme.md file with the changes.