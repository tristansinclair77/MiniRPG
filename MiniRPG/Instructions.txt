You are GitHub Copilot inside Visual Studio 2022.

Modify Player.cs.

Requirements:
• Add nullable Item properties:
   - Item? EquippedWeapon
   - Item? EquippedArmor
   - Item? EquippedAccessory
• Add method EquipItem(Item item):
   - If !item.IsEquippable → return false
   - Switch(item.SlotType):
       * "Weapon": EquippedWeapon = item
       * "Armor": EquippedArmor = item
       * "Accessory": EquippedAccessory = item
   - Recalculate effective stats:
       Attack = BaseAttack + (EquippedWeapon?.AttackBonus ?? 0)
       Defense = BaseDefense + (EquippedArmor?.DefenseBonus ?? 0)
   - Return true
• Add BaseAttack and BaseDefense fields to track original stat values separately.
• Update constructor to set BaseAttack = Attack, BaseDefense = Defense.
• Add TODO: // Add unequip logic and visual equipment preview later

---

After you finish, update the Readme.md file with the changes.