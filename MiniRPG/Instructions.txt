You are GitHub Copilot inside Visual Studio 2022.

Modify WorldMapViewModel.cs and MainViewModel.cs.

WorldMapViewModel:
• After TravelCommand is triggered:
   - If EncounterService.ShouldTriggerEncounter() == true:
       * Raise OnRandomEncounter(regionName) event.

MainViewModel:
• Subscribe to OnRandomEncounter.
• When raised:
   - Create new BattleViewModel(CurrentPlayer, regionName)
   - Log "A wild enemy appears during travel!"
   - Switch CurrentViewModel to BattleViewModel.
• After battle ends, return to target region’s MapViewModel.
• Add TODO:
   // Add encounter animations and travel interruption visuals

---

After you finish, update the Readme.md file with the changes.