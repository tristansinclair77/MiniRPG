You are GitHub Copilot inside Visual Studio 2022.

Modify MapViewModel.cs and MainViewModel.cs.

MapViewModel:
• Add ObservableCollection<NPC> NearbyNPCs.
• Populate with DialogueService.GetAllNPCs().
• Add RelayCommand named TalkToNPCCommand (parameter: NPC).

MainViewModel:
• Subscribe to TalkToNPCCommand event.
• When triggered:
   - Set CurrentViewModel = new DialogueViewModel(selectedNPC, CurrentPlayer)
• Subscribe to DialogueViewModel.OnDialogueExit → return to MapViewModel.
• Add TODO:
   // Add NPC proximity detection and movement system later

---

After you finish, update the Readme.md file with the changes.