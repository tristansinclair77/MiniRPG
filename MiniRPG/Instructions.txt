You are GitHub Copilot inside Visual Studio 2022.

Modify ShopViewModel.cs.

Requirements:
• On initialization:
   - var faction = Player.GetFaction(CurrentRegion.FactionName);
   - float modifier = faction.Reputation >= 50 ? 0.9f :
                      faction.Reputation <= -50 ? 1.2f : 1.0f;
   - Adjust displayed BuyPrice and SellPrice by modifier.
• If faction.Reputation < -50:
   - Disable BuyCommand (refuse to sell).
   - Log "They refuse to do business with you."
• Add TODO:
   // Add special item discounts for Allied factions

---

After you finish, update the Readme.md file with the changes.