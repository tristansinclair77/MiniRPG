You are GitHub Copilot inside Visual Studio 2022.

Create WorldMapViewModel.cs in the ViewModels folder.

Requirements:
• Inherit from BaseViewModel.
• Properties:
   - ObservableCollection<Region> Regions
   - Region? SelectedRegion
   - Player Player
• Commands:
   - TravelCommand
   - ExitWorldMapCommand
• Constructor(Player player):
   - Load Regions = new ObservableCollection(WorldMapService.GetRegions())
   - Set Player
• TravelCommand logic:
   - If SelectedRegion != null:
       * Trigger OnRegionSelected event with region info
       * Log $"Traveling to {SelectedRegion.Name}..."
• Add TODO:
   // Add travel cost, animation, and random encounter system

---

After you finish, update the Readme.md file with the changes.