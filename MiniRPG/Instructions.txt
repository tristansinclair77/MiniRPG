You are GitHub Copilot inside Visual Studio 2022.

Modify BuildingInteriorViewModel.cs.

Requirements:
• If CurrentBuilding.Type == "Inn":
   - Add RelayCommand named StayAtInnCommand.
   - When executed:
       * const int cost = 20;
       * If Player.Gold >= cost:
           Player.SpendGold(cost);
           Player.HP = Player.MaxHP;
           TimeService.AdvanceHours(8);
           Log "You rest at the inn and feel refreshed."
       * Else Log "You can't afford a room."
• Add TODO:
   // Add choice of standard/luxury rooms and longer rest durations

---

After you finish, update the Readme.md file with the changes.