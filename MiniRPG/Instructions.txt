You are GitHub Copilot inside Visual Studio 2022.

Modify Quest.cs.

Requirements:
• Add int? ExpireDay (null = no expiry)
• Method IsExpired() => ExpireDay.HasValue && TimeService.Day > ExpireDay.Value
• Modify Player.CompleteQuest to check if quest expired:
   - If expired, reward reduced or none.
• Add TODO:
   // Add quest timers displayed on quest board

---

After you finish, update the Readme.md file with the changes.