You are GitHub Copilot inside Visual Studio 2022.

Modify MapViewModel.cs and MainViewModel.cs.

MapViewModel:
• Add ObservableCollection<Building> RegionBuildings.
• Populate from current Region.Buildings.
• Add RelayCommand EnterBuildingCommand (parameter: Building).
• When executed, trigger OnEnterBuilding(Building).

MainViewModel:
• Subscribe to OnEnterBuilding.
• When triggered:
   - Set CurrentViewModel = new BuildingInteriorViewModel(selectedBuilding, CurrentPlayer).
• Subscribe to ExitBuilding event to return to MapViewModel.
• Add TODO:
   // Add animated fade transition between outdoor and indoor views

---

After you finish, update the Readme.md file with the changes.