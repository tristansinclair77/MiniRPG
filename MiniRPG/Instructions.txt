You are GitHub Copilot inside Visual Studio 2022.

Modify BuildingInteriorViewModel.cs and MainViewModel.cs.

BuildingInteriorViewModel:
• If CurrentBuilding.Type == "Blacksmith" or "Workshop":
   - Add RelayCommand OpenCraftingCommand.
   - Execute → trigger event OnOpenCrafting.

MainViewModel:
• Subscribe to OnOpenCrafting.
• When triggered:
   - Set CurrentViewModel = new CraftingViewModel(CurrentPlayer)
   - Log "Entered crafting menu."
• Subscribe to ExitCrafting event → return to BuildingInteriorViewModel.
• Add TODO:
   // Add special blacksmith NPC interactions before crafting

---

After you finish, update the Readme.md file with the changes.