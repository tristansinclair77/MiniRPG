You are GitHub Copilot inside Visual Studio 2022.

Create QuestBoardViewModel.cs in the ViewModels folder.

Requirements:
• Inherit from BaseViewModel.
• Properties:
   - ObservableCollection<Quest> AvailableQuests
   - Quest? SelectedQuest
   - Player Player
• Commands:
   - AcceptQuestCommand
   - ExitBoardCommand
• Constructor(Player player):
   - Initialize Player
   - Load AvailableQuests from QuestService.GetAvailableQuests()
• AcceptQuestCommand logic:
   - If SelectedQuest not null:
       * Player.AddQuest(SelectedQuest)
       * Remove from AvailableQuests
       * Log "You accepted the quest: [Title]"
• Add TODO:
   // Add quest filtering by type and region
   // Add quest completion check refresh

---

After you finish, update the Readme.md file with the changes.