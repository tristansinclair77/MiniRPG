You are GitHub Copilot inside Visual Studio 2022.

Modify Player.cs.

Requirements:
• Add property:
   - Dictionary<string, Faction> Factions { get; set; } = new();
• Add methods:
   - GetFaction(string name):
       * Return Factions[name] if exists, else create with default Reputation = 0.
   - AdjustFactionReputation(string name, int amount):
       * var faction = GetFaction(name);
       * faction.AdjustReputation(amount);
       * Log $"Faction {name} reputation changed to {faction.Reputation}."
• Add TODO:
   // Add global faction relationships (guilds, towns, bandits)

---

After you finish, update the Readme.md file with the changes.