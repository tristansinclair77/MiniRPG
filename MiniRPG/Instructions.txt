You are GitHub Copilot inside Visual Studio 2022.

Create EnvironmentService.cs in the Services folder.

Requirements:
• Static class EnvironmentService
• Static property string CurrentLighting = "Daylight"
• Method UpdateLighting():
   - switch(TimeService.GetTimeOfDay()):
       * "Morning" or "Afternoon" → "Daylight"
       * "Evening" → "Twilight"
       * "Night" → "Night"
• Trigger event OnLightingChanged whenever it updates.
• Add TODO:
   // Add regional weather, fog, and rain states later

---

After you finish, update the Readme.md file with the changes.