You are GitHub Copilot inside Visual Studio 2022.

Create FastTravelService.cs in the Services folder.

Requirements:
• Static class FastTravelService
• Static ObservableCollection<string> UnlockedRegions = new();
• Method UnlockRegion(string regionName):
   - If not already in UnlockedRegions, add it.
• Method IsUnlocked(string regionName) → bool
• Method GetUnlockedRegions() → returns copy of list.
• Add TODO:
   // Add cost, cooldown, and transport types (airship, portal, carriage)

---

After you finish, update the Readme.md file with the changes.