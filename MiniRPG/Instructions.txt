You are GitHub Copilot inside Visual Studio 2022.

Create CraftingViewModel.cs in the ViewModels folder.

Requirements:
• Inherit from BaseViewModel.
• Properties:
   - Player Player
   - ObservableCollection<CraftingRecipe> AllRecipes
   - CraftingRecipe? SelectedRecipe
• Commands:
   - CraftCommand
   - ExitCraftingCommand
• Constructor(Player player):
   - Load AllRecipes = new(CraftingService.GetAllRecipes()).
• CraftCommand:
   - If SelectedRecipe.CanCraft(Player):
       * SelectedRecipe.Craft(Player)
       * SaveLoadService.SavePlayer(Player)
       * Log success message.
   - Else Log "Missing materials or gold."
• ExitCraftingCommand triggers OnExitCrafting event.
• Add TODO:
   // Add bulk crafting and auto-select available recipes

---

After you finish, update the Readme.md file with the changes.