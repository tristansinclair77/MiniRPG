You are GitHub Copilot inside Visual Studio 2022.

Modify Player.cs.

Requirements:
• Add properties:
   - int SkillPoints { get; set; }
   - ObservableCollection<Skill> LearnedSkills { get; set; } = new();
   - ObservableCollection<Skill> AvailableSkills { get; set; } = new();

• Add method GainSkillPoints(int amount) => SkillPoints += amount;
• Add method UnlockSkill(Skill skill):
   - If SkillPoints >= skill.CostSP && !skill.IsUnlocked:
       * SkillPoints -= skill.CostSP
       * skill.IsUnlocked = true
       * LearnedSkills.Add(skill)
       * Log "Unlocked skill: {skill.Name}"
• In level-up logic (inside GainExperience or wherever applicable):
   - Add 1 SkillPoint per level gained.

• Add TODO:
   // Add skill respec (reset) feature later

---

After you finish, update the Readme.md file with the changes.