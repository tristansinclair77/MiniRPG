You are GitHub Copilot inside Visual Studio 2022.

Create TimeService.cs in the Services folder.

Requirements:
• Static class TimeService
• Static properties:
   - int Day = 1
   - int Hour = 8   // start morning
• Static methods:
   - AdvanceHours(int amount):
       * Hour += amount
       * If Hour >= 24:
           Day++
           Hour %= 24
   - GetTimeOfDay():
       * If Hour < 6 → "Night"
       * If Hour < 12 → "Morning"
       * If Hour < 18 → "Afternoon"
       * Else → "Evening"
• Add TODO:
   // Add moon phases, seasons, and weather events later

---

After you finish, update the Readme.md file with the changes.