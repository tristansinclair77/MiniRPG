You are GitHub Copilot inside Visual Studio 2022.

Modify Player.cs.

Requirements:
• Add ObservableCollection<Quest> ActiveQuests = new();
• Add ObservableCollection<Quest> CompletedQuests = new();
• Add method AddQuest(Quest quest) => ActiveQuests.Add(quest);
• Add method CompleteQuest(Quest quest):
   - Move quest from ActiveQuests to CompletedQuests
   - AddGold(quest.RewardGold)
   - GainExperience(quest.RewardExp)
   - If quest.RewardItem != null → AddItem(quest.RewardItem)
   - Log reward message.
• Add TODO: // Later - add quest categories (Main, Side, Daily)

---

After you finish, update the Readme.md file with the changes.