You are GitHub Copilot inside Visual Studio 2022.

Modify DialogueViewModel.cs.

Requirements:
• When AcceptQuestCommand executes:
   - If CurrentNPC.OfferedQuest not already in Player.ActiveQuests:
       * Player.AddQuest(CurrentNPC.OfferedQuest)
       * Append to GlobalLog: $"Quest accepted: {CurrentNPC.OfferedQuest.Title}"
       * Call SaveLoadService.SavePlayer(Player)
• Add TODO: // Add voiced confirmation and quest acceptance animation later

---

After you finish, update the Readme.md file with the changes.