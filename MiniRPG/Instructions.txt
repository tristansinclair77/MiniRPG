You are GitHub Copilot inside Visual Studio 2022.

Edit MapViewModel.cs.

Requirements:
• Add RelayCommand named EquipItemCommand.
• Command parameter: selected Item.
• When executed:
   - If item.IsEquippable:
       * bool equipped = Player.EquipItem(item);
       * If equipped: Log $"You equipped {item.Name}."
       * Else Log "Cannot equip this item."
   - Else Log "That item is not equippable."
• After equipping, call SaveLoadService.SavePlayer(Player).
• Add TODO: // Add equipment change sound effect later

---

After you finish, update the Readme.md file with the changes.