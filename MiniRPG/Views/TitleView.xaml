<UserControl x:Class="MiniRPG.TitleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <!-- TODO: Replace title with logo art -->
    <!-- TODO: Add background music and pixel-art intro scene -->
    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding NewGameCommand}" />
        <KeyBinding Key="C" Command="{Binding ContinueCommand}" />
    </UserControl.InputBindings>
    <Grid Background="#222233">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1" Text="Simple RPG" FontSize="48" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,32" />
        <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <Button Content="New Game" Command="{Binding NewGameCommand}" Width="220" Height="56" FontSize="22" Margin="0,0,0,16"
                    ToolTip="Enter: New Game" Foreground="#222233" Background="#F9E97A" FontWeight="Bold">
                <!-- TODO: Animate buttons on hover -->
            </Button>
            <Button Content="Continue" Command="{Binding ContinueCommand}" Width="220" Height="56" FontSize="22"
                    ToolTip="C: Continue" Foreground="#222233" Background="#F9E97A" FontWeight="Bold">
                <!-- TODO: Animate buttons on hover -->
            </Button>
        </StackPanel>
    </Grid>
    <!-- TODO: Add keyboard navigation and controller input later. -->
</UserControl>
